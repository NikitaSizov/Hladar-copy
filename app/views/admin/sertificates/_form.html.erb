<%= form_for([:admin, @admin_sertificate]) do |f| %>
  <% if @admin_sertificate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_sertificate.errors.count, "error") %> prohibited this admin_sertificate from being saved:</h2>

      <ul>
      <% @admin_sertificate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
