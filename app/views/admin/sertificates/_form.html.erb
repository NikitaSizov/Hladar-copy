<div class="modal-dialog">
  <div class="modal-content">
    <%= form_for([:admin, @sertificate], multipart: true, authenticity_token: true) do |f| %>
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Введите ваши данные</h4>
      </div>
      <div class="modal-body">
        <% if @sertificate.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@sertificate.errors.count, "error") %> prohibited this admin_sertificate from being saved:</h2>

            <ul>
            <% @sertificate.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
        <div class="form-group">
          <%= f.label :name, "Название: " %>
          <%= f.text_field :name, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label :image, "Новая картинка:" %><br>
          <%= f.file_field :image, class: "form-control"  %>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <%= f.submit "Отправить", class: "btn btn-default" %>
      </div>
    <% end %>
  </div>

</div>
